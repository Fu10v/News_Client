# News_Client
A fast and private desktop news reader client developed in C++ and Qt (QML). Supports integration with NewsAPI, local storage in SQLite, fast filtering, and reading articles via a built-in WebView.
